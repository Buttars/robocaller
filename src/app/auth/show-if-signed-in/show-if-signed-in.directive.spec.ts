import { ShowIfSignedInDirective } from './show-if-signed-in.directive';

describe('ShowIfSignedInDirective', () => {
  it('should create an instance', () => {
    const directive = new ShowIfSignedInDirective();
    expect(directive).toBeTruthy();
  });
});
